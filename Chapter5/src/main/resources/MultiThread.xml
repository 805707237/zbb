<?xml version="1.0" encoding="UTF-8" ?>
<suite name="多线程" parallel="classs" thread-count="2">
    <!--
    parallel(设定级别)
    thread-count(线程数量)
    tests级别：  不同的test tag下的用例可以在不同的线程下执行
                相同的test tag下的用例只能在同一个线程中执行
    classs级别：相同的class tag下的用例在同一个线程中执行
    methosd级别：所有的用例都在不同的线程下执行
    xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池
    -->
    <test name="demo2">
        <classes>
            <class name="com.course.testng.suite.MultiThreadOnXml"/>
        </classes>
    </test>

    <test name="demo3">
        <classes>
            <class name="com.course.testng.suite.MultiThreadOnXml"/>
        </classes>
    </test>
</suite>